# Nav Digital Studio — App (Next.js + TS + Tailwind)
Monochrome, x-ray-aesthetic portfolio that converts visitors into booked calls.
## Quickstart
```bash
npm install
npm run dev
# http://localhost:3000
```
Set your env vars from `.env.example` for analytics, email, and security.
